ЕЦТ — внешний автокаталог v2.7.29 (исправление Out of Memory)

Что исправлено:
- больше не кладётся тяжёлый индекс в localStorage старого формата
- компактное хранение, поиск чанками (UI не зависает)
- внешний поиск только от 3 символов и только если локально < 8 результатов
- предзагрузка всей базы при открытии каталога отключена
- лимит выдачи 12 карточек

Обязательно:
1) Залейте catalog-external.js, app.js, index.html, config.js
2) В браузере: DevTools → Application → Local Storage → удалите ключи
   ect_carsbase_v1 и ect_carsbase_ts_v1 (если есть)
3) Жёсткое обновление / инкогнито

Проверка: поиск «Tang» или «Chazor» — сайт не должен падать.
