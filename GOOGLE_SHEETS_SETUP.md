# Подключение Google Таблицы к ЕЦТ Скрипты

Таблица: https://docs.google.com/spreadsheets/d/10gitlvnBGl9i-wXtBZU3yfSCtu5SiIfitKAMeu0GweA/edit

## Как устроено хранение (важно!)

Все скрипты лежат **одним JSON в ячейке A1** листа `data`.

| Ячейка | Содержимое |
|--------|------------|
| **A1** | Полный JSON: `{"scripts":[...],"updatedAt":...,"version":1}` |
| **B1** | Краткая сводка (число скриптов) — только для удобства |
| **C1** | Время последней записи |

**Не добавляйте скрипты отдельными строками** в таблице — сайт их не увидит.
Редактирование идёт через сайт, либо аккуратная правка JSON в A1.

Сайт ↔ таблица:
- **Сайт → таблица**: при сохранении скрипта уходит POST на Apps Script
- **Таблица → сайт**: сайт каждые ~12 сек делает GET и подтягивает изменения

---

## Шаг 1. Лист
1. Открой таблицу
2. Переименуй первый лист в `data` (или оставь как есть)
3. A1 можно оставить пустым — сайт сам запишет JSON

## Шаг 2. Apps Script
1. **Расширения → Apps Script**
2. Удали код по умолчанию
3. Вставь содержимое файла `Code.gs`
4. **Сохранить**, имя проекта: `ECT Scripts API`

## Шаг 3. Развёртывание
1. **Развернуть → Новое развёртывание**
2. Тип: **Веб-приложение**
3. Описание: `ECT v1`
4. Запуск от имени: **От моего имени**
5. Кто имеет доступ: **Все** (Anyone / Anyone, even anonymous)
6. **Развернуть** → разреши доступ
7. **Скопируй URL** вида `https://script.google.com/macros/s/XXXX/exec`

> После **любых** правок Code.gs нужно:
> **Развернуть → Управление развёртываниями → карандаш → Новая версия → Развернуть**
> (иначе сайт продолжит ходить на старую версию)

## Шаг 4. На сайте
1. Войди **не** под «Общая» (Александр и др.)
2. **Настройки → Облако**
3. Вставь URL веб-приложения
4. **Подключить / Сохранить**
5. Статус в шапке должен стать **«Облако ✓»**

## Проверка
1. На сайте создай тестовый скрипт → через несколько секунд в A1 появится JSON
2. Нажми **Синхронизировать** (или подожди ~12 сек) на другом устройстве — скрипт появится
3. GET в браузере: открой URL `/exec` — должен вернуться JSON

## Типичные ошибки
| Симптом | Причина |
|---------|---------|
| Добавил «строку» в таблице — на сайте нет | Данные только в A1 как JSON |
| Сайт пишет, таблица пустая | Старое развёртывание / неверный URL / доступ не «Все» |
| Таблица есть, сайт не видит | Облако не подключено, или аккаунт «Общая», или жди auto-sync / F5 |
| CORS / ошибка облака | Content-Type должен быть `text/plain` (уже в app.js) |
